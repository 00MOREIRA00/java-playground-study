# Type Casting 

Se trata da conversão de tipos de variaveis para um outro tipo. O casting em Java pode correr de duas formas, e são elas:

* Implicito: ocorre quando a conversão é feita de um tipo menor para um maior (menos chance de perda de dados).
```java
 
```

* Explicito: ocorre quando a conversão é feito do tipo maio para o menor (pode ocorrer perda de dados)

